# ==========================================
# REQUIREMENTS - BACKEND (contenedor Docker / servidor headless)
# Python 3.13. NO incluye PySide6 (la GUI es el cliente de escritorio, que
# se ejecuta nativo, no en el contenedor). Añade psycopg2 (driver PostgreSQL).
#
# PyTorch CPU se instala APARTE en el Dockerfile con su index-url especial:
#   pip install torch==2.6.0+cpu torchvision==0.21.0+cpu \
#       --index-url https://download.pytorch.org/whl/cpu
# ==========================================

# ----- Core NumPy (Python 3.13 requiere 2.x, Ultralytics requiere <=2.1.1) -----
numpy==2.1.1

# ----- Visión por Computadora -----
opencv-python-headless==4.11.0.86   # headless: sin libGL de escritorio (servidor)
Pillow==12.0.0

# ----- Inteligencia Artificial (Ultralytics YOLOv8) -----
ultralytics==8.3.75
ultralytics-thop==2.0.14
matplotlib==3.10.0
pandas==2.2.3
psutil==6.1.0
py-cpuinfo==9.0.0
PyYAML==6.0.2
scipy==1.15.0
seaborn==0.13.2
tqdm==4.67.1

# ----- Web Framework (Flask) -----
Flask==3.1.0
Flask-JWT-Extended==4.7.1
Flask-Cors==5.0.0
Werkzeug==3.1.3
flask-sock==0.7.0
simple-websocket==1.1.0

# ----- Base de Datos -----
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10   # driver PostgreSQL (postgresql+psycopg2://)
alembic==1.14.0           # migraciones (alembic.ini)

# ----- Red y Protocolos -----
requests==2.32.3
python-telegram-bot==21.10
wsdiscovery==2.1.2
onvif-zeep==0.2.12
zeep==4.3.1
lxml==5.3.0

# ----- Dependencias de Zeep (ONVIF) -----
attrs==25.1.0
isodate==0.7.2
platformdirs==4.3.6
requests-file==1.5.1
requests-toolbelt==1.0.0

# ----- QR Code y Utilidades -----
qrcode==7.4.2
pypng==0.20220715.0
python-dotenv==1.0.1
pydantic==2.10.4

# ----- Seguridad -----
cryptography==44.0.0

# ----- Fechas y timezone -----
pytz==2026.1.post1
tzdata==2025.3
python-dateutil==2.9.0.post0
