"""
Paquete `services` del cliente desktop: capa de servicios que habla con el
backend Flask. Contiene api_client (cliente HTTP/JWT — columna vertebral) y
playback_service (reproducción de grabaciones con VLC). No exporta símbolos.
"""
