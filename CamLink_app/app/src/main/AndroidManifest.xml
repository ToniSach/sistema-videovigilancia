<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Tipo específico exigido por Android 14 (API 34) para foreground services
         que mantienen sincronización de datos en red. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Para mostrar notificaciones en Android 13+ (debe pedirse en runtime también). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Google Play interpreta uses-permission CAMERA como si la cámara fuera
         REQUERIDA (filtra Chrome OS y tablets sin cámara). Declaramos
         required="false" para que la app sea instalable en cualquier dispositivo
         compatible — el escáner QR se desactiva si no hay cámara, sin crashear. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        android:usesCleartextTraffic="true">
        <!-- android:label y android:theme heredan del <application> de arriba
             (no hace falta repetirlos aquí — lint RedundantLabel). -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden">
            <!-- configChanges: al entrar/salir de pantalla completa forzamos
                 landscape/portrait. Sin esto Android RECREA la Activity en cada
                 giro → el fragment se reconstruye en portrait y el fullscreen
                 "rebota" solo (y se mataba la sesión WebRTC). Manejándolo aquí,
                 el giro NO recrea: el estado de fullscreen y el vídeo persisten. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Foreground service que mantiene el WebSocket de notificaciones
             abierto contra el servidor NVR para recibir alertas en LAN sin FCM. -->
        <service
            android:name=".service.NotificationWebSocketService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>

</manifest>