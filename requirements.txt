# ==========================================
# REQUIREMENTS - PYTHON 3.13 COMPATIBLE
# Sistema NVR - Videovigilancia LAN
# ==========================================

# IMPORTANTE: Instalar en este orden:
# 1. Primero PyTorch (requiere index-url especial):
#    pip install torch==2.6.0+cpu torchvision==0.21.0+cpu --index-url https://download.pytorch.org/whl/cpu
#
# 2. Luego el resto:
#    pip install -r requirements.txt
#
# ==========================================

# ----- Core NumPy (Python 3.13 requiere 2.x, Ultralytics requiere <=2.1.1) -----
numpy==2.1.1

# ----- Visión por Computadora -----
opencv-python==4.11.0.86
Pillow==12.0.0  # Compatibilidad con PyTorch 2.6

# ----- Inteligencia Artificial (Ultralytics YOLOv8) -----
# Nota: PyTorch debe estar pre-instalado (ver arriba)
ultralytics==8.3.75
ultralytics-thop==2.0.14
matplotlib==3.10.0
pandas==2.2.3
psutil==6.1.0
py-cpuinfo==9.0.0
PyYAML==6.0.2
scipy==1.15.0
seaborn==0.13.2
tqdm==4.67.1

# ----- UI Desktop (PySide6) -----
# Nota: Si falla shiboken6 en Windows, reinstalar:
# pip uninstall PySide6 shiboken6 -y && pip cache purge && pip install PySide6==6.8.2
PySide6==6.8.2

# ----- Web Framework (Flask) -----
Flask==3.1.0
Flask-JWT-Extended==4.7.1
Flask-Cors==5.0.0
Werkzeug==3.1.3

# ----- Base de Datos -----
SQLAlchemy==2.0.36

# ----- Red y Protocolos -----
requests==2.32.3
python-telegram-bot==21.10
wsdiscovery==2.1.2
onvif-zeep==0.2.12
zeep==4.3.1
lxml==5.3.0

# ----- Dependencias de Zeep (ONVIF) - Requeridas explícitamente -----
attrs==25.1.0
isodate==0.7.2
platformdirs==4.3.6
requests-file==1.5.1
requests-toolbelt==1.0.0

# ----- QR Code y Utilidades -----
qrcode==7.4.2
pypng==0.20220715.0  # Dependencia de qrcode
python-dotenv==1.0.1
pydantic==2.10.4

# ----- Seguridad -----
cryptography==44.0.0

# ----- Fechas y timezone (dependencias de pandas) -----
pytz==2026.1.post1
tzdata==2025.3
python-dateutil==2.9.0.post0